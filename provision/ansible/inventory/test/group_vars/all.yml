---
# Global variables for TEST environment

# PostgreSQL credentials (should be in vault in production)
postgres_password: "test_password_change_me"

# LiteLLM configuration
litellm_master_key: "sk-litellm-test-key-change-me"

# Vault variables (these should be encrypted with ansible-vault)
# For now, using placeholder values for testing
vault_better_auth_secret: "test-better-auth-secret-32-bytes-long-change-me"
vault_sso_jwt_secret: "test-sso-jwt-secret-32-bytes-long-change-me"
vault_resend_api_key: ""  # Add your Resend API key here or in vault

# LiteLLM Database Configuration
litellm_enable_database: false  # Disabled until Prisma issues resolved
# litellm_database_url: "postgresql://{{ postgres_user }}:{{ postgres_password }}@{{ postgres_host }}:{{ postgres_port }}/litellm_test"
