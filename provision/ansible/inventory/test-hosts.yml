# Busibox Test Environment Inventory
# Auto-generated from test-vars.env
# 
# Container IDs: 301-307 (Production + 100)
# IP Range: 10.96.201.24-30
# Prefix: TEST-

all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_python_interpreter: /usr/bin/python3
    
    # Test environment flag
    busibox_env: test
    busibox_test_mode: true

    proxy_host: 10.96.201.200
    proxy_port: 8000
    litellm_base_url: http://{{ proxy_host }}:{{ proxy_port }}

    agent_host: 10.96.201.202
    agent_port: 8000

    # Service configuration (using test IPs)
    postgres_host: 10.96.201.203
    postgres_port: 5432
    postgres_db: busibox_test
    postgres_user: busibox_test_user
    
    milvus_host: 10.96.201.204
    milvus_port: 19530
    
    minio_endpoint: 10.96.201.205:9000
    minio_console_port: 9001
    
    redis_host: 10.96.201.206
    redis_port: 6379
    
    
  children:
    proxy:
      hosts:
        10.96.201.200:
          container_id: 300
          container_name: TEST-proxy-lxc

    apps:
      hosts:
        10.96.201.201:
          container_id: 301
          container_name: TEST-apps-lxc
    
    agent:
      hosts:
        10.96.201.202:
          container_id: 302
          container_name: TEST-agent-lxc

    pg:
      hosts:
        10.96.201.203:
          container_id: 303
          container_name: TEST-pg-lxc
    
    milvus:
      hosts:
        10.96.201.204:
          container_id: 304
          container_name: TEST-milvus-lxc
    
    files:
      hosts:
        10.96.201.205:
          container_id: 305
          container_name: TEST-files-lxc

    ingest:
      hosts:
        10.96.201.206:
          container_id: 306
          container_name: TEST-ingest-lxc
    
    
