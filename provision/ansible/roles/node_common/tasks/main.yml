- name: Install Node.js 20
  shell: |
    curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
    apt-get install -y nodejs git jq
  args: { executable: /bin/bash }

- name: Create apps root
  file: { path: /srv/apps, state: directory, mode: "0755" }
