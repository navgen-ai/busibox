// PM2 Ecosystem Configuration
// Generated by Ansible for {{ app_name }}

module.exports = {
  apps: [{
    name: '{{ app_name }}',
    script: '{{ start_script | default("npm") }}',
    args: '{{ start_args | default("start") }}',
    cwd: '{{ deploy_path }}',
    instances: {{ instances | default(1) }},
    exec_mode: '{{ exec_mode | default("fork") }}',
    watch: false,
    max_memory_restart: '{{ max_memory | default("1G") }}',
    env: {
      NODE_ENV: 'production',
      PORT: {{ port }},
{% if app_name == 'ai-portal' %}
      INGEST_API_HOST: '{{ ingest_ip }}',
{% endif %}
    },
    error_file: '/var/log/pm2/{{ app_name }}-error.log',
    out_file: '/var/log/pm2/{{ app_name }}-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_restarts: 10,
    min_uptime: '10s',
    listen_timeout: 3000,
    kill_timeout: 5000,
  }]
};

