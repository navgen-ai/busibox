---
# Ingest Service Default Variables

# GPU Configuration
# CUDA_VISIBLE_DEVICES for ingest container (default: GPU 0)
# Marker and ColPali share GPU 0 by default
ingest_cuda_visible_devices: "0"

# Virtual environment directory
ingest_venv_dir: "/srv/ingest/venv"

# Model registry configuration
# Path where model registry JSON is deployed to containers
model_registry_path: "/etc/ingest/model_registry.json"

# LLM cleanup configuration
# Enabled by default to fix text quality issues (smashed words, spacing, etc.)
llm_cleanup_enabled: true

