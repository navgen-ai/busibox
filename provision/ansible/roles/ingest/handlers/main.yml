---
- name: Restart ingest API
  systemd:
    name: ingest-api
    state: restarted
    daemon_reload: yes

- name: Restart ingest worker
  systemd:
    name: ingest-worker
    state: restarted
    daemon_reload: yes

- name: Reload nginx
  systemd:
    name: nginx
    state: reloaded

